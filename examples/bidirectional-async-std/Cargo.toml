[package]
name = "bidirectional-async-std"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
async-std = { version = "1.13.2", features = ["attributes"] }
async-trait = "0.1.89"
mcp-router = { path = "../.." }
serde = { version = "1.0.0", features = ["derive"] }
serde_json = "1.0.0"
