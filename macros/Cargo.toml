[package]
name = "macros"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true

[dependencies]
syn = {version="2", features=["full"]}
quote = "1"
proc-macro2 = "1"
inventory = "0.3"
registry = { path = '../registry' }
serde_json = "1.0.0"
tracing = "0.1.44"
